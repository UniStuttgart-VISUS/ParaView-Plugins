cmake_minimum_required(VERSION 3.12)

pv_plugin(${PROJECT_NAME} "b_spline;connect_lines;connect_points;open_closed_lines;point_cells;remove_long_segments;smooth_lines;sort_line_points;truncate_lines")
